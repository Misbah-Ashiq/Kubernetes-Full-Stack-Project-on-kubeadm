server {
  listen 80;

  # React app serve karo
  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    try_files $uri /index.html;
  }

  # Backend API ko proxy karo
  location /api/ {
    proxy_pass http://backend:5000/;
  }
}

